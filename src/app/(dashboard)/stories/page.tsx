import { StoriesWorkspace } from "@/components/stories/stories-workspace";

export const metadata = {
  title: "Story of My Life — LifeOS",
  description: "Tulis kisah hidup per stage usia, lengkap dengan AI teman curhat.",
};

export default function StoriesPage() {
  return <StoriesWorkspace />;
}
